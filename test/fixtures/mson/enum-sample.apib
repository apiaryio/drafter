# Data Structures

## User (object)
- e1: 3,4 (enum) 

- e2: 3,4 (enum, sample) 

- e3: *3,4* (enum) - FIXME: must be handled by different way in snowcrash

- e4 (enum) - nontyped enum, samples should be by default string
    - sample
        - 3
        - 4

- e5 (enum[number]) - typed enum -> samples && default should be numbers
    - sample
        - 3
        - 4
    - default
        - 1
    - sample
        - 5
        - 6

- e6 (enum) - nontyped enum, by default string, but overided by type specification on content members
    - pavan (string)
    - 4 (number)

- e7 (enum) - nontyped enum, by default string, but overided by type specification on content members
    - pavan (string)
    - *4* (number)
