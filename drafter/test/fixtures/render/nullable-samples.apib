# GET /

+ Response 200 (application/json)
    + Attributes
        + bar: a (nullable, sample)
        + barbar: 42 (number, nullable, sample)
        + arab: true (boolean, nullable, sample)
        + ba (array, nullable)
            + Sample
               + rbo
               + ra
        + bara (object, nullable)
            + Sample
               + raba: rab
               + bara: b
